name: stardist-cpu
channels:
  - conda-forge
  - defaults
dependencies:
  - python=3.10.13
  - tensorflow=2.18.0=cpu_py310h37b7b6b_0  # Specific version for CPU
  - numpy=1.24.4  # Compatible version with TensorFlow 2.18
  - stardist=0.8.3
  - tifffile=2024.2.12  # Required for reading/writing TIFFs
  - openjdk=8.0.412  # Necessary for some TensorFlow dependencies
  - pip=24.0
  - pip:
    - csbdeep==0.7.3  # Compatible version with StarDist 0.8.x
    - keras==2.18.0  # Aligned with TensorFlow 2.18
